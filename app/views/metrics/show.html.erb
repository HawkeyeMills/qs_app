<p id="notice"><%= notice %></p>
<%= render 'shared/error_messages', object: @metric %>
<p>
  <strong>MetricDate:</strong>
  <%= @metric.metricdate %>
</p>

<p>
  <strong>Value:</strong>
  <%= @metric.value %>
</p>

<%= link_to 'Edit', edit_metric_path(@metric) %> |
<%= link_to 'Back', metric_path %>
