<%= form_for(@metric_grade) do |f| %>
  <% if @metric_grade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@metric_grade.errors.count, "error") %> prohibited this metric_grade from being saved:</h2>

      <ul>
      <% @metric_grade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :metric_id %><br>
    <%= f.number_field :metric_id %>
  </div>
  <div class="field">
    <%= f.label :grade_id %><br>
    <%= f.number_field :grade_id %>
  </div>
  <div class="field">
    <%= f.label :points %><br>
    <%= f.number_field :points %>
  </div>
  <div class="field">
    <%= f.label :percentage %><br>
    <%= f.number_field :percentage %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
